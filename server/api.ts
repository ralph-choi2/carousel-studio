/**
 * DEPRECATED — 이 파일은 현재 참조되지 않음.
 *
 * 런타임 라우팅은 `vite.config.ts` 의 `createApiApp()` 에서 직접 처리됩니다.
 * 스크립트 데이터 read/write 는 Apps Script 웹앱으로 이관되었고 (src/lib/data-loader.ts),
 * PNG Export 라우트 `/api/export` 만 서버 사이드에 남아 있습니다.
 *
 * 히스토리 레퍼런스용으로 유지. 새 엔드포인트 추가 시에는 vite.config.ts 를 수정하세요.
 */
export {};
