# 이 파일을 `.env` 로 복사한 뒤 값을 채워 사용한다. `.env` 는 gitignore 됨.
#   cp .env.example .env

# fal.ai API key — 팀 공유 1개 키 사용.
# 관리자에게 별도 채널로 요청받아 아래에 붙여넣기.
FAL_API_KEY=

# (선택) Drive 대상 폴더 ID. 미지정시 기본값 재사용.
# CAROUSEL_DRIVE_FOLDER_ID=1W9RcqlEjik-O-Jr83l-mfFXco2E6Gaqa
